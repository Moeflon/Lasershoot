<!-- Display the countdown timer in an element -->

 <!DOCTYPE html>
<html>
<body>

<h1>My First Heading</h1>
<p>My first paragraph.</p>
<p id="demo"></p>


<script>

  var countDownDate = 0;
  var xhttp = new XMLHttpRequest();
  xhttp.onreadystatechange = function() {
    if (this.readyState == 4 && this.status == 200) {
      var countDownDate = this.responseText; 
    }
  };
  xhttp.open("GET", "http://193.191.169.45:8000/round_start", true);
  xhttp.send();

// Set the date we're counting down to

// Update the count down every 1 second
var x = setInterval(function() {

  // Get todays date and time
  var now = new Date().getTime();

  // Find the distance between now an the count down date
  var distance =  (now - countDownDate);

  // Time calculations for days, hours, minutes and seconds
  var minutes = (Math.floor((distance % (1000 * 60 * 60)) / (1000 * 60)) % 2);
  var seconds = 60 - Math.floor((distance % (1000 * 60)) / 1000);

  // Display the result in the element with id="demo"
  document.getElementById("demo").innerHTML = minutes + "m " + seconds + "s ";

}, 1000);
</script>
<div class="container">
  <div class="row justify-content-center mt-5">
    <div class="display-4 text-center">
      ENTER CODE
    </div>
  </div>
  <div class="row justify-content-center mt-4">
    <div class="col-md-6">
      <form method="post" action="">

        <input class="form-control" name="code" type="number"><br/>
        <input class="btn btn-success btn-block" type="submit" value="Login">
      </form>
    </div>
  </div>
</div>
</body>
</html> 
